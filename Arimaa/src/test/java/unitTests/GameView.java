package unitTests;

import static org.mockito.Mockito.mock;

public class GameView {
    public void testGameView(){
        GameView gameView = mock(GameView.class);
        gameView.testGameView();
    }
}
